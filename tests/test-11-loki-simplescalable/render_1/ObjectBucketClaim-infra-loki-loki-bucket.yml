---
# Source: helm-chart-of-the-death/templates/monitoring/loki/objectbucketclaim.yaml
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: loki-bucket
  namespace: infra-loki
  annotations:
    helm.sh/resource-policy: keep
spec:
  additionalConfig:
    #maxObjects: "10000"
    maxSize: 30G
  bucketName: loki-bucket
  generateBucketName: loki-bucket
  storageClassName: ceph-bucket
