---
# Source: helm-chart-of-the-death/templates/monitoring/push-gateway/helmrelease.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: push-gateway
  namespace: infra-push-gateway
  annotations:
    helm.sh/resource-policy: keep
spec:
  dependsOn:
    - name: prometheus
      namespace: infra-prometheus
  # include common.fluxcd.release
  install:
    crds: Create
    remediation:
      retries: 10
  interval: 1h
  test:
    enable: false
  timeout: 5m
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  chart:
    spec:
      chart: prometheus-pushgateway
      # each major contains upgrade process in changelog
      version: "3.6.0"
      sourceRef:
        kind: HelmRepository
        name: "prometheus-community"
        namespace: flux-system
      interval: 1h
  values:
    serviceMonitor:
      enabled: true
      namespace: infra-push-gateway
    networkPolicy:
      customSelectors:
        - namespaceSelector:
            matchLabels:
              network-policy-push-gateway: "true"
