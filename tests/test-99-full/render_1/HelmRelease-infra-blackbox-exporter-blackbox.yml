---
# Source: helm-chart-of-the-death/templates/monitoring/blackbox-exporter/helmrelease.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: blackbox
  namespace: infra-blackbox-exporter
  annotations:
    helm.sh/resource-policy: keep
spec:
  dependsOn:
    - name: prometheus
      namespace: infra-prometheus
  # include common.fluxcd.release
  install:
    crds: Create
    disableSchemaValidation: true
    remediation:
      retries: 10
  interval: 1h
  test:
    enable: false
  timeout: 5m
  upgrade:
    crds: CreateReplace
    disableSchemaValidation: true
    remediation:
      retries: 3
  chart:
    spec:
      chart: prometheus-blackbox-exporter
      # each major contains upgrade process in changelog
      version: "11.9.0"
      sourceRef:
        kind: HelmRepository
        name: "prometheus-community"
        namespace: flux-system
      interval: 1h
  values:
    resources:
      requests:
        cpu: 10m
        memory: 25M
    serviceMonitor:
      enabled: true
      selfMonitor:
        enabled: true
    prometheusRule:
      enabled: true
      rules:
        - alert: HttpCodeNode200
          expr: avg by (namespace, instance) (probe_http_status_code!=200)
          for: 1m
          labels:
            severity: critical
          annotations:
            summary: HTTP probe response code is not 200
            description: "HTTP probe response code {{ $value }} is not 200, on {{ $labels.instance }}"
